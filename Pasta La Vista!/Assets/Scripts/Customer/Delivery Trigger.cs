using UnityEngine;

public class DeliveryTrigger : MonoBehaviour
{
    /*void OnTriggerStay(Collider other)
    {
        if (other.CompareTag("Player") && Input.GetKeyDown(KeyCode.E))
        {
            if (GameObject.Find("Baked_Pizza") != null)
            {
                Customer customer = FindObjectOfType<Customer>();
                if (customer != null)
                {
                    customer.TakePizza();
                    Destroy(GameObject.Find("Baked_Pizza"));
                }
            }
        }
    }
    */
}
